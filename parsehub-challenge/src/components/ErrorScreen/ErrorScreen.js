import React from "react";
import { Typography, Grid } from "@material-ui/core";

function ErrorScreen(props) {
  return (
    <Typography>
      Error! You are redirected here because an invalid url has been entered.
    </ Typography>
  );
}

export default ErrorScreen;
