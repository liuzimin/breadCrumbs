import React from 'react';

function ErrorScreen(props) {

  return (
    <div>
      <h1>Error! You are redirected here because an invalid url has been entered.
      </h1>
    </div>
  )
}

export default ErrorScreen;